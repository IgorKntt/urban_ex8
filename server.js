const math = require("./math.js");

console.log(`Выполнение функции "add (10, 15)" из подклюенного модуля math.js: ${math.add(10, 15)}`);

console.log(`Выполнение функции "subtract(25, 10)" из подклюенного модуля math.js: ${math.subtract(25, 10)}`);

console.log(`Выполнение функции "multiply(5, 2))" из подклюенного модуля math.js: ${math.multiply(5, 2)}`);

console.log(`Выполнение функции "divide(40, 5) из подклюенного модуля math.js: ${math.divide(40, 5)}`);
